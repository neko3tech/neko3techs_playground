---
import { Image } from "astro:assets";

const { src, alt = "", width = 1920, height = 1080 } = Astro.props;
---

<Image src={src} alt={alt} width={width} height={height} quality="high" format="webp" {...Astro.props} />
